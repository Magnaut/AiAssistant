<Window x:Class="AiAssistantDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Michelle AI Assistant" Height="450" Width="600"
        Background="{DynamicResource BgBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🎨 Тема:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextBrush}"/>
                <ComboBox Name="cmbTheme" Width="120" SelectionChanged="CmbTheme_SelectionChanged"
                          Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}"/>
            </StackPanel>
            <TextBlock Name="lblStatus" Grid.Column="1" Text="🟢 Система активна" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="🤖 Модель:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextBrush}"/>
            <ComboBox Name="cmbModel" Width="150" SelectionChanged="CmbModel_SelectionChanged"
                      Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}"/>
        </StackPanel>

        <TextBox Name="txtLog" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" Padding="10" Margin="0,10" TextWrapping="Wrap"
                 Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource AccentBrush}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnToggleListen" Content="⏹ Остановить" Width="130" Height="35" Click="BtnToggleListen_Click" Margin="0,0,10,0"
                    Background="{DynamicResource ButtonBrush}" Foreground="{DynamicResource TextBrush}"/>
            <Button Name="btnClearLog" Content="🗑 Очистить" Width="100" Height="35" Click="BtnClearLog_Click"
                    Background="{DynamicResource ButtonBrush}" Foreground="{DynamicResource TextBrush}"/>
        </StackPanel>
    </Grid>
</Window>